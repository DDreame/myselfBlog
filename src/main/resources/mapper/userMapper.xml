<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="me.dddream.entity.User">
    <select id="getUserInfo" parameterType="java.lang.Integer" resultType="me.dddream.entity.User">
        select id, name,birthday
        from user
        where id=?
    </select>
</mapper>